# AI 认知大模型技术导引
> **版本**：0.2

本仓库按章节保存 Markdown 源稿、配图脚本、配图；最终发布产物是 `html/index.html`（响应式 HTML），`docs/` 下的 DOCX 作为参考副本。定位是**认知大模型技术和前景的入门导引，也教思路、养直觉、练技能**。

> **时效说明**：五书正文中的模型名、产品特性、benchmark 分数与性能数字，均为写作时点（2026-08）的事实快照；个别数字会随版本更新而变，但全书框架、机制讲解与推导不受影响。前沿结论的可靠程度，正文已用"推测""据信""主流假说""前沿未定"等词标明；各书附录参考文献中以 ★ 标记的 2026 年前沿工作，为下一版修订时优先复核的索引。

## 怎么看 AI：三个视角、三个问题

AI 没有唯一本质，不同学科各取一个视角，合起来用三个互补的视角看它，每个视角回答一个问题：

| 视角 | 问的问题 | 对应篇章 |
|:---|:---|:---|
| 动力学（数学/物理） | 模型是怎么造出来的？ | 《AI数学》《扩散》 |
| 几何与表示（数学） | 造出来后，学到的知识长什么样、怎么组织？ | 《基座模型》 |
| 社会-工程（工程/社会） | 把 AI 放进真实世界，怎么从有用变好用？ | 《用好AI》 |

三个问题连成一条追问链——怎么造、造出什么、怎么用。核心三步是《AI数学》→《基座模型》→《用好AI》：《AI数学》讲模型怎么造（训练是优化），《基座模型》讲造出来的知识长什么样（潜空间结构、能力涌现），《用好AI》讲怎么把 AI 用出可靠结果（后训练让模型可用、Harness 让系统可靠、使用有章法）。《扩散》是生成式 AI 的数学深入分支，从《AI数学》的分数匹配出发，讲另一条“怎么造”（生成是塑形）；它不是核心路径的前置，而是按需选读的深入篇。《AI law》是把三个问题里的“为什么”收拢成一条线的规律深入分支（从现象到规律到边界），前置《AI数学》与《基座模型》，读完核心三步后按需选读。

## 五篇文档 & 推荐阅读顺序

五篇文档**独立可读**，也构成一条“核心三步 + 两条选读分支”的路径。跨篇引用统一用书名简称：《AI数学》《扩散》《基座模型》《用好AI》《AI law》。

**核心路径（推荐按顺序读）**

1. **`1_ai_math/`** - AI数学：从起步到前沿（7 章 + 1 附录）
   数学工具链--梯度下降、拉格朗日、Softmax、反向传播、Attention、LoRA、MoE。为《基座模型》和《用好AI》提供数学基础，也是《扩散》的数学起点。前沿展望章节讲五场范式转移（混合架构、MoE、测试时算力、数据墙、扩散模型），收束到扩散模型作为数学桥梁。

2. **`2_foundation/`** - 基座模型：从咿呀到行动（6 章 + 2 附录）
   AI 的当前主流。萃取->筑基->炼灵->应变->行动->收束。覆盖预训练与潜空间、SFT 与对齐起点、推理算力与 GRPO、RLHF/DPO/Safe RL、KV Cache 与全双工、VLA 与具身智能、世界模型六流派。"概念城市"专题：潜空间可解释性与可干预性（SAE/电路追踪/知识编辑/激活引导/J-space/Latent Agents）。GPT-Live、DeepSeek-R1、Figure Helix 等 2024-2026 前沿。部分章内分 Part A/B（潜空间与预训练/多模态接入、推理与对齐/安全与落地、世界模型与认知架构/可解释性与应用），便于按需深入。

3. **`3_use_ai/`** - 用好AI：从有用到好用（8 章 + 结语 + 4 附录）
   AI 已经有用，怎么把它用好？三站旅程：有用 → 可用（后训练四种反馈：例题/打分/对错/干活）→ 好用（Harness 六层 × 使用章法）。POMDP 眼镜、Harness 六层与选型地图、控制范式、规划与 Skill 双刃性、四类失效、评估与护栏、数字与物理、多 Agent 协作（AgentFail 三层十三类）、自演化三路线与三硬边界、科研自动化三件套判据。附录《实践路径》给出从十行代码到生产级 Harness 的真实现场路线；附录《如何实现一个 Agent/Harness》以 DeepSeek Harness 为参照给出从零落地清单；附录《参考资料与延伸阅读》汇总全书引用与延伸阅读。

**选读分支（可放在核心路径之后，也可在读完对应前置后直接深入）**

- **`1a_diffusion/`** - 扩散：从噪声生成（9 章 + 4 个附录）
  生成式 AI 的另一条主线。从"加噪声"出发，一步步推导 SDE/ODE 框架、条件控制、离散扩散，收束到视频生成、世界模型和扩散策略。与《AI数学》共享数学基础（梯度→分数匹配），独立展开。结尾用"技法 × 工具"框架统一前沿变体（DiT、流匹配、一致性模型、SD3 工业配方、i1 全开源配方消融）。数学预备附录提供符号速查表与高斯/期望/梯度/ODE/反向传播条目。

- **`4_ai_law/`** - AI law：从现象到规律（8 章 + 4 个附录）
   深度学习为什么能工作、规律何时失效？分三拍组织：第一拍**看**现象与动力学；第二拍**懂**可外推规律与其机制；第三拍**判与望**——测量与边界、理论之梯回望（选读视角章，按需回望）。与《AI数学》《基座模型》共享数学基础，独立展开；《AI数学》讲“怎么造”，《基座模型》讲“造出什么”，本篇讲“为什么符合规律、何时失效”。《理论之梯：经典与现代理论如何推动 AI》是回望视角章（按需回望）梳理信息论、优化、统计学习、控制、博弈、因果、认知等十余条理论在 AI 中的真实贡献（目标/算法/结构/护栏四路），并用“正-反-合”讲清现代理论与现象的互相塑造。

## 自检怎么用

五篇的每章末尾都有"本章自检"。提示和答案的位置因发布产物而异，请在阅读时以当前产物版式为准：

- **HTML（`html/index.html`）**：提示与答案以折叠块形式放在各题旁边，先独立作答，需要时再点开。
- **DOCX（`docs/` 参考副本）**：提示保留在题目区，答案集中在文末答案区，以保持"先作答、再翻页对照"的隔离。

用法三步：

1. **先独立作答**（回想或默写），卡住再看提示——提示只给方向；
2. **对照答案**——答案句末的 §X.Y 就是回看正文的位置；
3. 自检有五种题型：知识点（回忆）、逻辑链（追溯链条）、迁移（换场景用）、边界（适用条件）、生成（画图/举例/列表）。生成题的答案自带"核对你的答案"清单，用它自评要点，不只对结论。

## 跨篇分工：概念地图

各篇之间有明确的分工边界，避免内容重复。《AI law》作为规律深化篇，与四篇的分工（物理层 vs 现象/工程层）在篇首定位句写明。下表把同一概念在不同文档中的位置标成“先读 → 深入 → 应用/延展”，方便按路径定位。

| 概念 | 首次/场景直觉 | 深入/机制 | 应用/延展 | 推荐顺序 |
|:---|:---|:---|:---|:---|
| **POMDP** | 《基座模型》讲机器人场景直觉（扫地机器人，POMDP 首现） | 《用好AI》完整七元组与信念手算、Harness 六层映射 | 《用好AI》四类失效归类、长程规划、多 Agent 根因 | 《基座模型》→《用好AI》（先场景直觉，再用起来） |
| **世界模型** | 《基座模型》六流派全景与胜负手 | 《扩散》扩散视角的 SDE 延伸 | 《扩散》扩散在路线之争中的位置；《用好AI》物理 Agent 约束 | 《基座模型》→《扩散》（先全景，再深入生成视角） |
| **扩散策略** | 《基座模型》VLA 与动作表示 | 《扩散》多峰动作分布 | 《用好AI》物理 Agent 约束 | 《基座模型》→《扩散》（先 VLA，再多峰动作） |
| **推理模型** | 《AI数学》数学联系（test-time compute scaling） | 《基座模型》三代演进与 GRPO 算法 | 《用好AI》RLVR 使用侧、规划成本与 Effort | 《AI数学》→《基座模型》→《用好AI》（数学→算法→使用） |
| **推理加速** | 《AI数学》落地表格给低秩定位 | 《基座模型》KV Cache 与加速全景（PagedAttention/推测解码/量化） | 《用好AI》Harness 选型、物理端云分层 | 《AI数学》→《基座模型》→《用好AI》（低秩→加速→选型） |
| **后训练** | 《用好AI》使用侧四种反馈（例题/打分/对错/干活）；《基座模型》算法起点 | 《基座模型》算法与训练细节（SFT/RLHF/DPO/GRPO/Safe RL） | 《用好AI》评估与护栏 | 先用《用好AI》建立使用直觉，再读《基座模型》算法细节；也可按核心路径先读《基座模型》 |

## 目录结构

```
.
├── 1_ai_math/           # 《AI数学》：源稿 .md + generate_figures.py + figures/
├── 1a_diffusion/        # 《扩散》：源稿 .md + generate_figures.py + figures/
├── 2_foundation/        # 《基座模型》：源稿 .md + generate_figures.py + figures/
├── 3_use_ai/             # 《用好AI》：源稿 .md + generate_figures.py + figures/
├── 4_ai_law/                 # 《AI law》：源稿 .md + generate_figures.py + figures/
├── html/                # 最终发布产物（index.html + 本地 KaTeX/figures）
├── docs/                # DOCX 参考副本（五篇）
├── build_html.py        # Markdown → HTML 最终发布构建脚本
├── build_docx.py        # Markdown → DOCX 参考副本构建脚本（Chapter 配置硬编码目录名）
├── style_docx.py        # DOCX 样式统一（字体/标题/表格/页眉/页码/图片尺寸）
├── fig_common.py        # 五篇配图脚本共享模块（CJK 字体探测/保存/rcParams）
└── requirements.txt     # 依赖（numpy、matplotlib、python-docx、lxml）
```

`references/`（根目录）存放写作时的参考资料（PDF/调研笔记），不参与构建、不入版本库。

## 生成发布产物

### 最终 HTML（推荐）

依赖：`pandoc`、`matplotlib`、`numpy`。

```bash
python3 build_html.py                # 生成 html/index.html
```

> 如果仓库中没有 `vendor/`，脚本会自动通过 `npm` 安装 KaTeX 和 highlight.js 到系统临时缓存（需要 Node.js/npm 和网络）。

生成流程：

1. `pandoc` 将五篇 Markdown 分别转为 HTML 片段。
2. `md_links.py` 修复目录锚点与跨篇链接。
3. `build_html.py` 合并为单个响应式 `html/index.html`，并复制本地 KaTeX/figures。

### DOCX 参考副本

依赖：`pandoc`、`python-docx`、`matplotlib`、`numpy`。

```bash
python3 build_docx.py              # 生成全部 DOCX 参考副本
python3 build_docx.py 1a_diffusion  # 只生成扩散模型章节
python3 build_docx.py --verify-only
```

生成流程：

1. `pandoc` 将章节 Markdown 转为 `*_raw.docx`（放在章节文件夹）。
2. 根目录 `style_docx.py` 统一字体、标题、表格、页眉、页码和图片尺寸。
3. DOCX 输出到 `docs/` 目录，成功后自动删除 `*_raw.docx` 中间文件。

`style_docx.py` 也可单独使用：

```bash
python3 style_docx.py --input path/to/input_raw.docx --output path/to/output.docx --fallback-title 文档标题
```

## 配图

- `fig_common.py`（根目录）：五篇配图脚本共享的基础模块——CJK 字体自动探测、统一保存函数、rcParams、批处理进度打印。
- `1_ai_math/generate_figures.py` 生成《AI数学》配图到 `1_ai_math/figures/`（20 张）。
- `1a_diffusion/generate_figures.py` 生成《扩散》配图到 `1a_diffusion/figures/`（41 张）。
- `2_foundation/generate_figures.py` 生成《基座模型》配图到 `2_foundation/figures/`（23 张）。
- `3_use_ai/generate_figures.py` 生成《用好AI》配图到 `3_use_ai/figures/`（18 张）。
- `4_ai_law/generate_figures.py` 生成《AI law》配图到 `4_ai_law/figures/`（14 张）。

依赖安装：`python3 -m pip install -r requirements.txt`（numpy、matplotlib、python-docx、lxml）。

### 色彩约定

五篇配图遵循 `fig_common.py` 共享语义色板的取色约定：

- **语义角色**：红=危险/错误（`ROLE_DANGER`），绿=正确/成功（`ROLE_SUCCESS`），橙=警告/强调（`ROLE_WARNING`），蓝=主色（`ROLE_PRIMARY`）；新图按此取色。
- **色相族三阶**：`BLUE/RED/GREEN/ORANGE` 各含 `l`（浅填充）、`m`（主体）、`d`（强调深色）三阶——浅=参考/背景，中=主体/默认，深=重点/证据。
- **灰阶**：`GRAY.l/m/d` 三个灰（浅填充/中灰边框/深灰文本）。
- **禁忌**：禁止在脚本中引入新的独点色（使用 ≤2 次的任意 HEX）；同族颜色一律从共享色板取色；对立语义（正确/错误、优势/劣势）的并置对比避免红绿直接对撞，用红/蓝或红/橙。
- **适用范围**：全库存量图与新增图统一遵守；改动任何配图时必须保持上述约定，不得引入新的独点色。
