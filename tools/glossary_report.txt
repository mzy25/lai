== 1) 词卡 vs 各书定义句（句内含术语+定义词；空=该书无定义句） ==
[token] 卡:模型读文本的最小单元；图文音视频都要先切 token 再进模型 | ·）输入矩阵   的每一行是一个token的向量表示 | ·- **吸收态（掩码）**：token 跳向一个特殊的 `[M]`（空白）且不再变回，逐步把序列擦成全 `[M]`— | ·| 表示形式 | 连续二维矩阵 | 离散 token 序列 | | ·开源项目 Pi（π）是这套框架的一个干净实现，六层各有明确模块：推理层抽象多厂商模型接口并管 token 预算；工具 | ·- Schaeffer 等 2023 换用连续指标（token 级别的概率）观察，能力是平滑增长的——"突然"是指标
[潜空间] 卡:模型内部的高维向量空间，知识按有序结构在其中组织 | ·— | ·> **跨篇心法呼应**：**扩散模型本质就是"在图像潜空间里做生成"**——先用 VAE 把像素压进潜空间，再在潜 | ·高维表示在潜空间里组织成有序结构，能力从结构里涌现 | ·— | ·**表示**（representation）是网络内部特征结构——潜空间里的方向——随训练更新的过程
[POMDP] 卡:部分可观测马尔可夫决策过程——信息不全、动作有后果、反馈有延迟 | ·— | ·— | ·— | ·> **时间紧可先读**：读 POMDP 七元组与信念状态两段（即“看 Harness 的眼镜”的核心），后续手算与 | ·—
[SFT] 卡:监督微调——用人工示范让模型学会特定场景的行为 | ·— | ·— | ·注意 SFT 的默认档位——投影器全量加 LLM 首尾层——恰好就是上文"再验证"给出的共识配方的微缩版 | ·你手上的产品不是从石头缝里蹦出来的：模型诞生时只会续写——**有潜能，但没成形**；**后训练**（post-tra | ·—
[RLHF] 卡:基于人类反馈的强化学习——用偏好数据对齐模型 | ·— | ·— | ·**回指 Ch3**：RLHF/DPO 可以在训练层缓解幻觉（教模型说"我不知道"），但如果 benchmark 不 | ·你手上的产品不是从石头缝里蹦出来的：模型诞生时只会续写——**有潜能，但没成形**；**后训练**（post-tra | ·1. **"理论只会说不行，AI 靠工程"**——交叉熵、SGD、CNN、RLHF 都早已落地到工程实践，本就是理论
[DPO] 卡:直接偏好优化——不训练奖励模型的对齐方法 | ·— | ·— | ·**回指 Ch3**：RLHF/DPO 可以在训练层缓解幻觉（教模型说"我不知道"），但如果 benchmark 不 | ·你手上的产品不是从石头缝里蹦出来的：模型诞生时只会续写——**有潜能，但没成形**；**后训练**（post-tra | ·—
[GRPO] 卡:群组相对策略优化——用同组样本的相对优势更新策略 | ·— | ·— | ·aha moment 的直接推手就是 GRPO：DeepSeek-R1 跳过 SFT，直接用 GRPO 做 RL，模 | ·— | ·—
[RLVR] 卡:可验证奖励强化学习——奖励来自规则（如测试通过）而非偏好 | ·— | ·— | ·— | ·你手上的产品不是从石头缝里蹦出来的：模型诞生时只会续写——**有潜能，但没成形**；**后训练**（post-tra | ·—
[Safe RL] 卡:把安全当作硬约束的强化学习 | ·— | ·— | ·**回指**：Safe RL 就是拉格朗日乘子法在 RL 中的应用——约束是"安全代价不超过阈值"，拉格朗日乘子   | ·— | ·—
[KV Cache] 卡:缓存注意力键值，让生成不重复计算 | ·— | ·— | ·这就是为什么大模型推理服务的显存瓶颈往往是 KV Cache 而不是模型参数 | ·— | ·—
[流式生成] 卡:token 一生成就输出，不等整句完成 | ·— | ·— | ·— | ·— | ·—
[VLA] 卡:视觉-语言-动作模型（机器人控制） | ·| **具身智能** | VLA（Vision-Language-Action，视觉-语言-动作）模型的本质就是把" | ·> **跨篇分工**：VLA 视角的机器人控制见《基座模型》——RT-2 动作 token 化（§5.3）、π0 f | ·> VLA（视觉-语言-动作模型）让 LLM 输出的不是文字 token，而是电机指令 | ·主流两条路线——VLA 把视觉和语言指令映射为离散动作 bin；扩散策略直接学动作分布，天然处理多峰任务——两者不互 | ·—
[Sim-to-Real] 卡:从模拟器到现实世界的迁移 | ·— | ·— | ·> **本章路线**：先看 VLA 与动作表示，再看数据采集与 Sim-to-Real，接着看分层决策、语音具身与物 | ·送货机器人、手术机械臂、自动驾驶，跑的仍是同一个 POMDP 循环、同一套六层，但四个数字世界不存在的约束会改写每一 | ·—
[grokking] 卡:延迟泛化——训练误差先为零，测试误差后突然跃迁 | ·— | ·— | ·— | ·— | ·我们从可复现的现象出发（双下降、grokking、涌现），还原背后机制（优化动力学、特征学习、表示结构），提炼能外推
[双下降] 卡:测试误差随容量先降、再升、尖峰、骤降、平台 | ·— | ·— | ·— | ·— | ·我们从可复现的现象出发（双下降、grokking、涌现），还原背后机制（优化动力学、特征学习、表示结构），提炼能外推
[两速度分解] 卡:表示是慢变量、读出是快变量的动力学视角 | ·— | ·— | ·— | ·— | ·本章先提出问题，给出经典答案，再指出它失效的地方，最后给出正在成形的统一视角——两速度分解
[隐式正则化] 卡:优化过程本身带来的正则效果（如 SGD 噪声） | ·— | ·— | ·— | ·— | ·这条"隐形的力量"就是第 1.7 节的隐式正则化
[尺度定律] 卡:损失随规模按幂律下降、可外推的规律 | ·— | ·— | ·— | ·— | ·我们从可复现的现象出发（双下降、grokking、涌现），还原背后机制（优化动力学、特征学习、表示结构），提炼能外推
[SAE] 卡:稀疏自编码器——从叠加中恢复可解释特征的工具 | ·— | ·— | ·SAE（稀疏自编码器，§2.1 首见）探测实验发现：模型内部的**推理特征在对应 token 生成之前就已经激活** | ·— | ·** 先给规律、再拆机制（拍内顺序不变）：尺度定律一章先兑现第一条能外推的规律——尺度定律——并摆出三个起源候选（数
[叠加] 卡:特征方向多于维度时方向必然彼此重叠的现象 | ·— | ·这条路在工程上很有吸引力：不动权重意味着零训练成本，可以即插即用地叠加到任何预训练扩散模型上 | ·— | ·一个 20 步的任务，如果用 ToT 走 3 路探索（3 倍并行开销）、每路调用推理模型（推理 token 单价约为 | ·** 先给规律、再拆机制（拍内顺序不变）：尺度定律一章先兑现第一条能外推的规律——尺度定律——并摆出三个起源候选（数
[induction 电路] 卡:匹配-复制结构的可重复执行子电路 | ·— | ·— | ·— | ·— | ·** 先给规律、再拆机制（拍内顺序不变）：尺度定律一章先兑现第一条能外推的规律——尺度定律——并摆出三个起源候选（数
[μP] 卡:最大更新参数化：让最优学习率不随宽度缩放 | ·— | ·— | ·— | ·— | ·** 先给规律、再拆机制（拍内顺序不变）：尺度定律一章先兑现第一条能外推的规律——尺度定律——并摆出三个起源候选（数
[NTK] 卡:神经正切核：无穷宽度极限下网络退化为核回归 | ·— | ·— | ·— | ·— | ·—
[MoE] 卡:专家混合——按 token 稀疏激活子网络 | ·用类比来理解：MoE就是**大厨房的分工制度** | ·— | ·后接式没有消亡——Qwen3-VL（235B MoE（混合专家），开源 VLM 头部水平，MMMU 87.1%（写作 | ·— | ·—
[LoRA] 卡:低秩微调——冻结权重、只训练低秩残差 | ·这就是 LoRA 的数学基础：  的形式天然能表达秩   矩阵 | ·— | ·— | ·— | ·—
[SVD] 卡:奇异值分解——低秩世界的数学工具 | ·Ch5的注意力机制中，  里的   是一般线性变换，它的SVD分解（本章即将介绍）中包含正交矩阵   和   作为旋 | ·— | ·— | ·— | ·—
[RoPE] 卡:旋转位置编码——让注意力感知顺序 | ·**这就是 RoPE 的核心**：两个 token 的相对距离决定了旋转角度差，角度差决定内积——所以注意力分数只依 | ·— | ·— | ·— | ·—
[Softmax] 卡:把分数归一成概率的“诚实选择” | ·答案就是下一章的 Softmax：一个"诚实的"概率输出 | ·— | ·Query 和所有 Key 做点积得到相关度分数，Softmax 归一化成权重，用权重加权所有 Value——得到这 | ·— | ·—
[交叉熵] 卡:衡量预测分布与真实分布的编码代价 | ·考完对答案时，你"自信地猜错"的懊悔感特别强烈——这就是**交叉熵**：猜得越离谱，惩罚越重 | ·— | ·训练目标和预训练完全一样——交叉熵损失（§2.2），只是数据从"互联网文本"换成了"人类标注的指令-回答对" | ·— | ·| Shannon 熵（1948） | 交叉熵/对数似然：逻辑回归与神经网络的经典损失，今天仍是语言模型的训练目标 
[KKT] 卡:不等式约束最优性的互补松弛条件 | ·KKT条件里有一条至关重要的规则，叫做**互补松弛性**（Complementary Slackness）： | ·— | ·— | ·— | ·—
[DDPM] 卡:去噪扩散概率模型——VP 调度的离散化采样 | ·— | ·DDPM 用的就是这种 | ·— | ·— | ·—
[DDIM] 卡:确定性采样——ODE 的离散化，去除随机性 | ·— | ·DDIM 就是这条 ODE 的离散化（下一节 §5.2） | ·— | ·— | ·—
[CFG] 卡:引导缩放——放大条件信号、加强生成控制 | ·— | ·分数方向反过来就是噪声方向，得到  ，就是 (§6.3) 的 CFG 公式 | ·— | ·— | ·—
[DiT] 卡:用 Transformer 替换 U-Net 的扩散架构 | ·当前的前沿正在模糊这条边界：Emu3（智源，*Nature* 2025）证明纯 next-token predict | ·它的多尺度结构在数据量不大时是优势（归纳偏置帮模型省数据），但当数据涨到几十亿张、算力涨到千卡集群规模时，这种强结构 | ·— | ·— | ·—
[流匹配] 卡:直接回归向量场的生成路线 | ·— | ·> **记号切换提示**：流匹配惯例与前 8 章不同——**  而非  ；  仍是数据，但   表示"路径终点"（通 | ·**回指**：π0 的 flow matching 和扩散模型中的流匹配数学完全一致，只是应用从图像生成变成了动作生 | ·— | ·—
[ControlNet] 卡:给扩散模型加条件控制的旁路结构 | ·— | ·— | ·— | ·— | ·—
[世界模型] 卡:能预测环境下一步状态的模型 | ·— | ·扩散世界模型学的就是这个条件分布 | ·世界模型、具身基础模型、认知架构——每个方向都回指前五章的能力，也都是全新问题的开始 | ·— | ·该章全章即工具箱：测量方法直接可用，对齐极限、世界模型与方法论三节为选读
[Harness] 卡:模型外部让 AI 可靠工作的整套系统 | ·— | ·— | ·**回指《用好AI》**：这五条局限正是 Harness 六层每一层存在理由——基座给能力上限，系统给稳定性下限 | ·> 三个词先认识一下：**后训练**＝让模型学会听指令、会推理、会用工具的训练阶段；**Agent**＝会规划、调工 | ·—
[Skills] 卡:规划产物的固化形态——可复用技能包 | ·— | ·— | ·— | ·Anthropic 推出的 **Agent Skills** 就是把稳定的规划模式固化为可复用工件：^[23]^ | ·—
[HITL] 卡:人在环——人类作为回路中的特殊节点 | ·— | ·— | ·— | ·— | ·—
[AgentFail] 卡:多 Agent 协作失败的根因分类 | ·— | ·— | ·— | ·AgentFail 的作者们还测出四条收敛的修复策略：格式验证模块（对齐 F1.2）、二次校验代理与备用工具路径（对 | ·—
[上下文工程] 卡:把高质量信息装进上下文窗口的工程 | ·— | ·— | ·— | ·上下文工程的核心痛点是**注意力预算**：即便上下文窗口扩到百万级，实证发现随着输入增长，模型对远端信息的注意力会衰 | ·—
[鞍点] 卡:梯度为零但不是极值的点 | ·— | ·— | ·— | ·— | ·高维景观的第一个事实是：**驻点里，极小值指数级稀少，鞍点是常态**
[Effort] 卡:想多久——推理算力分配（努力度） | ·— | ·— | ·— | ·— | ·—

== 2) 同书疑似二次定义(术语+定义词 的句数>1；可能需引用式瘦身) ==
  1_ai_math [token]: 21 句
  1_ai_math [MoE]: 4 句
  1_ai_math [LoRA]: 7 句
  1_ai_math [SVD]: 7 句
  1_ai_math [RoPE]: 4 句
  1_ai_math [Softmax]: 37 句
  1_ai_math [交叉熵]: 12 句
  1a_diffusion [token]: 7 句
  1a_diffusion [DDPM]: 11 句
  1a_diffusion [DDIM]: 7 句
  1a_diffusion [CFG]: 4 句
  1a_diffusion [DiT]: 6 句
  1a_diffusion [流匹配]: 4 句
  1a_diffusion [世界模型]: 5 句
  2_foundation [token]: 35 句
  2_foundation [潜空间]: 18 句
  2_foundation [SFT]: 8 句
  2_foundation [RLHF]: 4 句
  2_foundation [DPO]: 2 句
  2_foundation [GRPO]: 3 句
  2_foundation [Safe RL]: 3 句
  2_foundation [KV Cache]: 2 句
  2_foundation [VLA]: 9 句
  2_foundation [Sim-to-Real]: 2 句
  2_foundation [Softmax]: 3 句
  2_foundation [世界模型]: 4 句
  3_use_ai [token]: 3 句
  3_use_ai [POMDP]: 6 句
  3_use_ai [SFT]: 4 句
  3_use_ai [RLHF]: 3 句
  3_use_ai [DPO]: 3 句
  3_use_ai [RLVR]: 3 句
  3_use_ai [VLA]: 2 句
  3_use_ai [Harness]: 9 句
  3_use_ai [Skills]: 3 句
  3_use_ai [上下文工程]: 2 句
  4_ai_law [token]: 3 句
  4_ai_law [潜空间]: 3 句
  4_ai_law [RLHF]: 2 句
  4_ai_law [grokking]: 16 句
  4_ai_law [双下降]: 7 句
  4_ai_law [两速度分解]: 6 句
  4_ai_law [尺度定律]: 5 句
  4_ai_law [SAE]: 6 句
  4_ai_law [叠加]: 11 句
  4_ai_law [μP]: 3 句
  4_ai_law [交叉熵]: 4 句
  4_ai_law [世界模型]: 3 句
  4_ai_law [鞍点]: 2 句
  小计: 49

== 3) 覆盖缺口(跨书高频词未入卡, top 14) ==
  details x808
  summary x808
  times x550
  Agent x521
  sigma x330
  nabla x287
  lambda x285
  partial x260
  varepsilon x232
  arXiv x225
  alpha x208
  theta x205
  approx x195
  Attention x184